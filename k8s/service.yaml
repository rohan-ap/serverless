apiVersion: serving.knative.dev/v1 
kind: Service 
metadata:
  name: echo
  namespace: pipeline-serverless
spec:
  template:
    spec:
      containers: quay.io/rohanp/private-demo
        - image: 
          ports:
            - containerPort: 8080 
          env: 
            - name: MY_VAR
              value: "12345"
